# The pre-built kits this tool needs. A hosting service reads this file to know
# what to install when it puts your app on the internet.

anthropic        # talks to the Claude AI
python-dotenv    # reads your secret key from the private .env file
streamlit        # turns the tool into a web page people can open in a browser
